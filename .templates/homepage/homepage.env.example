TZ=Europe/London
#PUID=1000
#PGID=1000

# This pulls from your GLOBAL .env (root) and gives it to Homepage
#   Here are some examples to use - change  values there to 
#    fill into homepages services  easily or fill directly here as well
# ! if remote_ip not set docker will chose host_ip automatcally

# Use ' HOMEPAGE_ALLOWED_HOSTS=* ' if having auth troubles with ip/hostname etc..
HOMEPAGE_ALLOWED_HOSTS=${SYSTEM_HOSTNAME}:3000,${SYSTEM_HOSTNAME}.local:3000,${HOST_IP}:3000,localhost:3000,${REMOTE_HOSTNAME}.local:3000

# Use for all apps if they all run on  same addresses/pi's
HOMEPAGE_VAR_APP_REMOTE_HOSTNAME=${REMOTE_HOSTNAME}
HOMEPAGE_VAR_APP_REMOTE_IP=${REMOTE_IP}

# =============================================================================
#  PER-APP ENVIRONMENT VARIABLES FOR SERVICES.YAML
# =============================================================================
# If running diff specific hostnames or ips use your own vars 
# here and use these examples within homepages configs(services widgets etc) files as needed 

# ── MEDIA ARR STACK ──────────────────────────────────────────────────────────
#HOMEPAGE_VAR_SONARR_HOSTNAME=${REMOTE_HOSTNAME:-$REMOTE_IP}
#HOMEPAGE_VAR_SONARR_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_SONARR_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_RADARR_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_RADARR_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_LIDARR_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_LIDARR_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_PROWLARR_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_PROWLARR_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_JACKETT_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_JACKETT_KEY=YOUR_PASSWORD_OR_KEY_HERE

# ── DOWNLOADERS & MEDIA STREAMING ────────────────────────────────────────────
#HOMEPAGE_VAR_QBITTORRENT_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_QBITTORRENT_USERNAME=admin
#HOMEPAGE_VAR_QBITTORRENT_PASSWORD=adminadmin

#HOMEPAGE_VAR_PLEX_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_PLEX_KEY=YOUR_PLEX_TOKEN_HERE

#HOMEPAGE_VAR_JELLYFIN_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_JELLYFIN_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_SEERR_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_SEERR_KEY=YOUR_API_KEY_HERE

# ── PHOTO & FILE PACKS ───────────────────────────────────────────────────────
#HOMEPAGE_VAR_IMMICH_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_IMMICH_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_IMMICH_POWER_TOOLS_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_PHOTOPRISM_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_PHOTOPRISM_KEY=YOUR_PASSWORD_HERE

#HOMEPAGE_VAR_SYNCTHING_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_SYNCTHING_KEY=YOUR_API_KEY_HERE

# ── SMART HOME & UTILITIES ───────────────────────────────────────────────────
#HOMEPAGE_VAR_HOMEASSISTANT_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_HOMEASSISTANT_KEY=YOUR_LONG_LIVED_ACCESS_TOKEN_HERE

#HOMEPAGE_VAR_MOSQUITTO_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_HOMEBOX_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_TRILIUM_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_TRILIUM_KEY=YOUR_TOKEN_HERE

#HOMEPAGE_VAR_VAULTWARDEN_IP=${REMOTE_IP:-$HOST_IP}

# ── MONITORING & MANAGEMENT ──────────────────────────────────────────────────
#HOMEPAGE_VAR_UPTIMEKUMA_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_UPTIMEKUMA_USERNAME=admin
#HOMEPAGE_VAR_UPTIMEKUMA_KEY=YOUR_PASSWORD_HERE

#HOMEPAGE_VAR_TAUTULLI_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_TAUTULLI_KEY=YOUR_API_KEY_HERE

#HOMEPAGE_VAR_GLANCES_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_DOCKSENTRY_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_PORTAINER_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_PORTAINER_KEY=YOUR_API_KEY_HERE

# ── CORE NETWORK & INGRESS ────────────────────────────────────────────────────
#HOMEPAGE_VAR_CADDY_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_PIHOLE_IP=${REMOTE_IP:-$HOST_IP}
#HOMEPAGE_VAR_PIHOLE_KEY=YOUR_WEBPASSWORD_OR_API_TOKEN_HERE

#HOMEPAGE_VAR_WIREGUARD_IP=${REMOTE_IP:-$HOST_IP}

# ── ALTERNATIVE DASHBOARDS / PROXIES ─────────────────────────────────────────
#HOMEPAGE_VAR_HOMARR_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_HEIMDALL_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_FLARESOLVERR_IP=${REMOTE_IP:-$HOST_IP}

#HOMEPAGE_VAR_ITTOOLS_IP=${REMOTE_IP:-$HOST_IP}