  
#---------------------------------------------------------------------#
#     LIDARR UI                                                       #
#---------------------------------------------------------------------#  
  
  lidarr:
    image: linuxserver/lidarr
    container_name: lidarr
    env_file:
      - ./services/lidarr/lidarr.env    
    volumes:
      - ${VOLUMES_DIR}/lidarr/config:/config
      #modify below paths to your setups folders choices in portainer + pi/linux mounted drives in 'fstab
      - /media/USB_PLEX_14TB/MUSIC:/music
      - /media/USB_PLEX_14TB/DOWNLOADS:/downloads
    ports:
      - 8686:8686
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8686/UI/Dashboard"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s  
    labels:
      - homepage.group=MEDIA
      - homepage.name=lidarr
      - homepage.icon=lidarr.png
      - homepage.href=http://${SYSTEM_HOSTNAME}:8686
      - homepage.description=song manager  
    networks:
      private_network:
        
